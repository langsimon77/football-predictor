"""football-predictor: EPL and La Liga match predictions."""

from pathlib import Path

# Repository root, so every module finds data/ and docs/ the same way.
ROOT = Path(__file__).resolve().parents[2]
